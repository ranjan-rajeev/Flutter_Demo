import 'package:flutter/material.dart';

class AppColors {
  Color primaryColorLight = Colors.blue[300];
  Color primaryColor = Colors.blue[500];
  Color primaryColorDark = Colors.blue[700];
  Color primaryColorAccent = Colors.pinkAccent;
}
